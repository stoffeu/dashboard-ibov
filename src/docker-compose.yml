version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: ibov_dashboard_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ibov_dashboard
    ports:
      - "5432:5432"
    volumes:
      - ibov_dashboard_data:/var/lib/postgresql/data

volumes:
  ibov_dashboard_data:
